import "./UserInboxPage.scss";

const UserInboxPage = () => {
  return (
    <main className="user-inbox-page">
      <section className="user-inbox-page-container">
        <h1 className="user-inbox-page-title"> user-inbox page </h1>
      </section>
    </main>
  );
};

export default UserInboxPage;
